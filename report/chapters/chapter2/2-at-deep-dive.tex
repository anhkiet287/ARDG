\section{Adversarial Training Deep Dive}

% TODO[ĐA]:
% Goal:
% - Cover the attack algorithms and adversarial-training concepts needed for Chapters 4-6.
% Flow:
% - For each attack: intuition -> threat model/norm -> update or objective if needed -> role in this report.
% - Then explain how adversarial training uses attacks inside the training loop.
% Include:
% - One attack-summary table with columns: Attack, Norm, Iterative?, Targeted?, Used for training?, Used for evaluation?, Role.
% - FGSM-RS, PGD-$\ell_\infty$, CW-$\ell_2$, DeepFool-$\ell_2$, DDN-$\ell_2$, MI-FGSM, TPGD, and AutoAttack-$\ell_\infty$.
% Avoid:
% - Overlong derivations or code-level attack settings that belong in Chapter 5.
% Target length: 3-5 pages including the table.

\subsection{Min-max formulation}

% TODO[ĐA]:
% Goal:
% - Explain adversarial training as min-max optimization.
% Flow:
% - Inner maximization generates hard adversarial examples; outer minimization updates the model.
% - Explain why this improves robustness but can specialize to the attack used for training.
% Include:
% - Add an equation only if it is referenced later.
% - Distinguish PGD-$\ell_\infty$ as an attack from PGD-AT as a training method.
% - Distinguish DDN-$\ell_2$ as an attack from DDN-AT as a training method.
% Avoid:
% - Introducing AttackDRO++ details before Chapter 4.
% Target length: 4-6 paragraphs.

\subsection{PGD-AT and its limitations}

% TODO[ĐA]:
% Goal:
% - Explain why PGD-AT is a strong but narrow baseline.
% Flow:
% - Describe PGD-AT, then identify cross-attack and norm-specific limitations.
% Include:
% - Connect limitations to the need for Multi-AT and group-aware objectives.
% Avoid:
% - Treating PGD-$\ell_\infty$ and PGD-AT as interchangeable terms.
% Target length: 3-4 paragraphs.

\subsection{Multi-attack AT approaches}

% TODO[ĐA]:
% Goal:
% - Explain why multi-attack adversarial training is the natural next baseline.
% Flow:
% - Start from single-attack specialization, then explain mixed-source training.
% - End with the limitation that uniform mixing cannot emphasize weak groups.
% Include:
% - Use `Multi-AT` as the report-facing method name.
% Avoid:
% - Claiming Multi-AT is weak; it is the strong uniform baseline.
% Target length: 3-4 paragraphs.

\subsection{Trade-off analysis}

% TODO[ĐA]:
% Goal:
% - Explain the trade-offs among clean accuracy, robust accuracy, attack diversity, and stability.
% Flow:
% - Discuss why improving one attack or group can affect another.
% - Set up why Chapter 4 needs anchored adaptive reweighting.
% Include:
% - Mention that empirical trade-offs are evaluated in Chapter 6.
% Avoid:
% - Reporting Chapter 6 numerical results here.
% Target length: 2-3 paragraphs.
