% --------------------------------------------------------------------------
\section{Augmented Clustering with Gradient Fingerprints}
\label{sec:ch4_gradfp}

Representation-space clustering alone may not fully capture adversarial difficulty.
Two adversarial examples can be close in feature space while inducing different
optimization behavior. To capture this additional signal, AttackDRO++ constructs
an augmented clustering feature that combines representation similarity, class
information, and gradient-level training dynamics.

For an adversarial example \(\tilde{\mathbf{x}}_i=\mathbf{x}_i^{\mathrm{adv}}\), let
\[
z_i = f_\theta(\tilde{\mathbf{x}}_i),
\qquad
h_i = \phi_\theta(\tilde{\mathbf{x}}_i).
\]
The logits \(z_i=f_\theta(\tilde{\mathbf{x}}_i)\) are used to compute prediction
probabilities and gradient-fingerprint terms, while
\(h_i=\phi_\theta(\tilde{\mathbf{x}}_i)\) provides the representation component of
the clustering feature. Let
\[
p_i = \mathrm{softmax}(z_i)
\]
be the predicted class distribution.

The first component is the normalized representation feature,
\[
\bar{h}_i = \operatorname{norm}(h_i),
\]
which captures semantic similarity in the model feature space.

The second component is a class-aware scalar label feature. When label information
is enabled, the implementation appends the normalized scalar class-index feature
\(\tilde{y}_i \in [0,1]\) with weight \(\lambda_{\mathrm{lbl}}\):
\[
\lambda_{\mathrm{lbl}}\tilde{y}_i,
\]
which provides weak label structure during clustering without using a one-hot label
vector in the clustering feature.

The third component is the gradient fingerprint. For a linear classifier head
\[
f_\theta(\cdot) = W\phi_\theta(\cdot) + b,
\qquad
W \in \mathbb{R}^{C \times d_h},
\]
the per-example gradient of the cross-entropy loss with respect to the classifier
weight matrix is
\[
\nabla_W \ell(f_\theta(\tilde{\mathbf{x}}_i),y_i)
=
\bigl(p_i-\mathrm{onehot}(y_i)\bigr)\otimes h_i
\in \mathbb{R}^{C\times d_h}.
\]
We use this quantity as a gradient proxy and denote it by
\[
G_i =
\bigl(p_i-\mathrm{onehot}(y_i)\bigr)\otimes h_i.
\]
The one-hot vector appears here only in the cross-entropy gradient proxy; it is
not appended as the label component of \(\psi_i\).

Intuitively, \(G_i\) describes the training direction induced by sample \(i\).
Thus, two adversarial examples may be grouped together not only because they are
close in representation space, but also because they induce similar optimization
updates.

The raw gradient proxy has dimension \(C d_h\). For CIFAR-10 with a ResNet-18
backbone, this corresponds to \(10 \times 512 = 5120\) dimensions. To make the
fingerprint more compact and efficient for clustering, we use a fixed random
projection matrix
\[
P \in \mathbb{R}^{C d_h \times d_{\mathrm{proj}}},
\]
and define the projected gradient fingerprint as
\[
g_i = \mathrm{vec}(G_i)P
\in \mathbb{R}^{d_{\mathrm{proj}}}.
\]
In our default setting, \(d_{\mathrm{proj}}=128\). The projection is fixed throughout
training and introduces no additional learnable parameters.

The final augmented clustering feature is
\[
\psi_i =
\left[
\operatorname{norm}(h_i)
\;\Vert\;
\lambda_{\mathrm{lbl}}\tilde{y}_i
\;\Vert\;
\lambda_{\mathrm{grad}}\operatorname{norm}(g_i)
\right].
\]

This construction keeps the clustering feature compact while still encoding information
that is not visible from the representation alone. The representation term captures where
the adversarial example lies in the learned feature space, but two nearby points may still
produce different loss gradients if the classifier is uncertain in different directions. The
projected gradient fingerprint provides a lightweight summary of this optimization
behavior, while the scalar label feature preserves coarse class structure during clustering.
The weights \(\lambda_{\mathrm{lbl}}\) and \(\lambda_{\mathrm{grad}}\) control how much label
and gradient information influence the final grouping relative to the normalized
representation feature.

\begin{figure}[H]
\centering
\resizebox{0.55\textwidth}{!}{%
\begin{tikzpicture}[
    node distance = 0.65cm,
    >=Latex,
    font=\small,
    every node/.append style={align=center},
    block/.style={
        draw,
        rounded corners,
        thick,
        align=center,
        minimum width=5.0cm,
        minimum height=1.15cm,
        text width=5.2cm,
        fill=blue!8,
        inner sep=4pt
    },
    smallblock/.style={
        draw,
        rounded corners,
        thick,
        align=center,
        minimum width=5.0cm,
        minimum height=1.1cm,
        text width=5.2cm,
        fill=green!8,
        inner sep=4pt
    },
    merge/.style={
        draw,
        rounded corners,
        thick,
        align=center,
        minimum width=5.0cm,
        minimum height=1.2cm,
        text width=5.2cm,
        fill=orange!12,
        inner sep=4pt
    },
    outblock/.style={
        draw,
        rounded corners,
        thick,
        align=center,
        minimum width=5.0cm,
        minimum height=1.1cm,
        text width=5.2cm,
        fill=purple!8,
        inner sep=4pt
    },
    line/.style={-Latex, thick},
    groupbox/.style={draw, dashed, rounded corners, inner sep=10pt},
    sideboxlabel/.style={font=\footnotesize, rotate=90, anchor=south}
]

\node[block] (input)
{Adversarial sample\\[2pt] $x_i^{\mathrm{adv}}$};

\node[block, below=0.7cm of input] (model)
{Training model\\[2pt]
$f_\theta = g_\theta \circ \phi_\theta$\\[-1pt]
};

\node[smallblock, below=1.0cm of model] (rep)
{Backbone feature\\[2pt]
$h_i=\phi_\theta(x_i^{\mathrm{adv}})$};

\node[smallblock, below=0.55cm of rep] (logits)
{Classifier output\\[2pt]
$z_i=f_\theta(x_i^{\mathrm{adv}})$};

\node[smallblock, below=0.55cm of logits] (grad)
{Gradient fingerprint\\[2pt]
$g_i=\mathrm{vec}(G_i)P$\\[-1pt]
};

\node[merge, below=1.0cm of grad] (aug)
{Canonical augmented\\clustering feature\\[2pt]
$\psi_i=[\operatorname{norm}(h_i);$\\[-1pt]
$\lambda_{\mathrm{lbl}}\tilde{y}_i;$\\[-1pt]
$\lambda_{\mathrm{grad}}\operatorname{norm}(g_i)]$};

\node[outblock, below=0.65cm of aug] (cluster)
{K-means\\clustering};

\node[outblock, below=0.65cm of cluster] (assign)
{Latent group\\assignment\\[2pt]
$c_i \in \{1,\dots,K\}$};

\draw[line] (input) -- (model);

\coordinate (signalStem) at ($(model.south)+(0,-0.45)$);
\coordinate (splitRep) at ($(rep.west)+(-0.65,0)$);
\coordinate (splitLogits) at ($(logits.west)+(-0.65,0)$);
\coordinate (splitGrad) at ($(grad.west)+(-0.65,0)$);
\draw[thick] (model.south) -- (signalStem);
\draw[thick] (signalStem) -| (splitRep);
\draw[thick] (splitRep) -- (splitGrad);
\draw[line] (splitRep) -- (rep.west);
\draw[line] (splitLogits) -- (logits.west);
\draw[line] (splitGrad) -- (grad.west);

\coordinate (mergeRep) at ($(rep.east)+(0.65,0)$);
\coordinate (mergeLogits) at ($(logits.east)+(0.65,0)$);
\coordinate (mergeGrad) at ($(grad.east)+(0.65,0)$);
\draw[thick] (rep.east) -- (mergeRep);
\draw[thick] (logits.east) -- (mergeLogits);
\draw[thick] (grad.east) -- (mergeGrad);
\draw[thick] (mergeRep) -- (mergeGrad);
\draw[line] (mergeLogits) |- (aug.north);

\draw[line] (aug) -- (cluster);
\draw[line] (cluster) -- (assign);

% Add invisible right anchors so the dashed group boxes extend further right.
\coordinate (signalsRightPad) at ($(mergeGrad)+(0.75,0)$);
\coordinate (latentRightPad) at ($(aug.east)+(0.75,0)$);

\node[groupbox, fit=(rep)(logits)(grad)(splitRep)(signalsRightPad),
      label={[sideboxlabel, xshift=0.25cm]right:Signals from the same training model}] {};

\node[groupbox, fit=(aug)(cluster)(assign)(latentRightPad),
      label={[sideboxlabel, xshift=0.25cm]right:Latent group discovery}] {};

\end{tikzpicture}%
}
\caption[Gradient fingerprint clustering feature]{Augmented clustering feature construction in AttackDRO++. Each
adversarial sample \(x_i^{\mathrm{adv}}\) is passed through the same training
model \(f_\theta\), producing both the backbone feature \(h_i\) and classifier
output \(z_i\). A gradient fingerprint \(g_i\) is computed from the
classifier-head loss gradient and projected to a low-dimensional vector. The
representation feature, scalar normalized label feature, and gradient fingerprint
are combined as the canonical augmented clustering feature
\(\psi_i=[\operatorname{norm}(h_i);
\lambda_{\mathrm{lbl}}\tilde{y}_i;
\lambda_{\mathrm{grad}}\operatorname{norm}(g_i)]\), which is then used for
K-means clustering and latent group assignment. Logits are used to compute
probabilities and gradient fingerprints, but they are not concatenated directly
into \(\psi_i\).}
\label{fig:gradfp_augmented_feature}
\end{figure}
